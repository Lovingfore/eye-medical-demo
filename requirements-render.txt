# Render uses CPU-only wheels to keep the web image small enough for a free
# instance. Local GPU training continues to use requirements.txt.
-f https://download.pytorch.org/whl/cpu
Pillow>=10.0,<12
numpy>=1.24,<3
scikit-learn>=1.3,<2
Django>=5.0,<6
torch==2.5.1+cpu
torchvision==0.20.1+cpu
gunicorn>=23.0,<24
whitenoise>=6.8,<7
